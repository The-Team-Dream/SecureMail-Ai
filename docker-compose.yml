services:
  ai:
    build:
      context: ..
      dockerfile: SecureMail-Ai/Dockerfile
    container_name: securemail_ai
    environment:
      GROQ_API_KEY: ${GROQ_API_KEY:-}
      GRPC_PORT: "50051"
      AI_MAX_CONCURRENT: "4"
    ports:
      - "50051:50051"
    restart: unless-stopped
